#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char str[100];
    // scanf("%s", str);  // Read a string from input
    int value = atoi(str);  // Convert string to integer
}